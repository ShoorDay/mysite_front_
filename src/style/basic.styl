@import './config.styl';

#footer {
    display: flex;
    background-color: rgb(52, 58, 64);
    color: #aaa;
    padding: 24px 50px;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}
